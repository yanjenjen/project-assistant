# 【F005-2】進度查詢（原「合約」，已與原F005-4合併） — 問題追蹤

> 開發／測試過程中發現、尚未解決或待使用者決策的問題。每則標日期，狀態用 待確認／待修／已解決／不處理。

## 2026-07-16

**決策**：F005-4「進度查詢」併入本功能（原「合約」分頁），合併後分頁名稱維持「進度查詢」。合併原因：兩者資料來源相同（都是 `kenda.signage`），欄位高度重疊。

**原懸案已釐清**：
- 「處理進度怎麼跟 BPM 同步」——答案是「狀態」(`state`) 跟「申請進度」(`apply_progress`) 其實是兩個不同欄位。`state` 是平台自己的流程狀態，現在就有在運作；`apply_progress` 才是要等建大 BPM 做好才串接的欄位，這次不處理，留空即可。
- 明細畫面欄位確認：全部是 `kenda.signage` model 既有欄位，不需新增。
- 開發範圍：確認在本專案範圍內，本輪只做**前台查詢**，不含 F005-4 原本規劃的後台 Excel 三段式維護機制（待未來 BPM 串接需求明確後再規劃）。
- 「距到期天數」沿用 F005-1 既有 `_compute_days_to_expire`，後端算好回傳。

**狀態**：已解決（範圍與欄位皆已確認，可進入開發）。

## 2026-07-13（舊，保留歷史）

**問題**：這個分頁原本編號誤植為「合約下載」（PDF 範本下載，已更正為 F006）。實際比對前端 bundle 後發現這裡顯示的是申請案合約狀態（車行/狀態/合約起始/合約到期），功能用途、資料來源、是否為本專案開發範圍都不確定，`00_主需求.md` 內容純屬逆向推導。
**狀態**：已解決，見上方 2026-07-16 紀錄。
