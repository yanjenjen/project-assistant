# 【F003】最新消息功能改造 — 測試紀錄

- **對應功能**：F003
- **日期**：2026-07-02
- **測試環境**：本機（localhost:8080 前台 ／ localhost:8071 後台）

## 測試項目與結果

| 項目 | 預期結果 | 實際結果 | 通過 |
|---|---|---|---|
| 模組升級（`-u kenda_platform`） | 無錯誤，新欄位 target_category_ids 建立、舊欄位 product_category 移除 | log 顯示成功刪除舊 Selection 欄位與選項，views 全部載入無錯誤 | 是 |
| 客戶主檔重新匯入（0625版） | dealer 資料 upsert 更新，不刪除既有主檔 | `dealer 更新 46`，`目前總數 dealers=49` | 是 |
| ORM 層過濾：消息顯示對象=客戶類別 | 客戶看得到該則消息 | R02012（AC類）看得到「顯示對象=AC」的測試消息 | 是 |
| ORM 層過濾：消息顯示對象≠客戶類別 | 客戶看不到該則消息 | R02012 看不到「顯示對象=其他類別」的測試消息 | 是 |
| ORM 層過濾：消息未設顯示對象 | 所有人可見（沿用原邏輯） | R02012 看得到「未設顯示對象」的測試消息 | 是 |
| 後台服務可連線 | HTTP 200 | `localhost:8071/web/login` → 200 | 是 |
| 前台服務可連線 | HTTP 200 | `localhost:8080/` → 200 | 是 |
| 前台新版 JS 載入 | HTTP 200 | `localhost:8080/assets/index-BnEs5qT0.v10.js` → 200 | 是 |
| 前台瀏覽器實際點擊（列表/詳細頁顯示對象標籤） | 待測 | 未測試 | 待補 |

## 發現問題

- 客戶主檔 0625 版的購買產品類別代碼（AC/BC/MC/OTHER）跟舊版（BC/MC/PCR/IC）不完全一致；匯入腳本本身是動態建表，不受影響，但代表舊資料裡可能同時存在新舊代碼混用的 `kenda.product.category` 紀錄（例如同時有 PCR 和 AC），需要之後確認是否要人工合併/清理，避免後台下拉選單出現重複/過時的類別。
- 這次只做到 ORM/API 層驗證，尚未用瀏覽器實際操作確認前台卡片與詳細頁的顯示對象標籤呈現效果。

## 是否已同步到客戶測試端

否——使用者明確指示這次先做到本機，不動 aiuptop.com。
